[package]
name = "azimuth-assurance-lifecycle"
version = "0.1.0"
edition = "2021"
publish = false
license = "Apache-2.0"

[dependencies]
azimuth-assurance-domain = { path = "../../services/assurance/domain" }
