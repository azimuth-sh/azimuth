[package]
name = "azimuth"
version = "0.1.0-alpha.2"
edition = "2021"
description = "Derives and validates an evidence-control-plane model."
license = "Apache-2.0"
readme = "README.md"
repository = "https://github.com/azimuth-sh/azimuth"
homepage = "https://azimuth.sh"
keywords = ["requirements", "traceability", "verification", "sdd"]
categories = ["command-line-utilities", "development-tools"]
rust-version = "1.85"
include = ["src/**", "Cargo.toml", "Cargo.lock", "README.md", "LICENSE"]

# No dependencies, by decision (D17). The destination is other people's CI.

[[bin]]
name = "azimuth"
path = "src/main.rs"

[lib]
name = "azimuth"
path = "src/lib.rs"
