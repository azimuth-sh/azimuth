# Model packages

This canonical framework repository owns no accepted consumer-domain intent. Model packages added
here must describe the framework itself; consumer specifications remain under their own intent
authority and are assembled through project federation when needed.

The directory exists so lifecycle commands can distinguish an intentionally empty current model
from a missing project layout.
